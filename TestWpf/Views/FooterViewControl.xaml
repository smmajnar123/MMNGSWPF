<UserControl x:Class="TestWpf.Views.FooterViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestWpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Border Background="{StaticResource FooterBrush}" Height="30" Padding="5" VerticalAlignment="Bottom">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Left side: App info -->
            <TextBlock Text="© 2025 MMNGS Application" 
                       VerticalAlignment="Center"
                       Foreground="Black"
                       FontSize="14"/>

            <!-- Right side: Version info -->
            <TextBlock Grid.Column="1"
                       Text="v1.0.0"
                       VerticalAlignment="Center"
                       Foreground="Black"
                       FontSize="14"/>
        </Grid>
    </Border>
</UserControl>
